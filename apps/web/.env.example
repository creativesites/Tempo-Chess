# Tempo Chess (web prototype) requires no API keys.
# All AI/coaching logic is offline/local — see apps/mobile for the on-device
# llama.cpp + Stockfish integration. This file is kept only because Vite
# expects an .env.example to exist; there is currently nothing to configure.
